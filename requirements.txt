streamlit>=1.49.0,<2.0.0
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<2.0.0
SQLAlchemy>=2.0.0,<3.0.0
bcrypt>=4.1.0,<5.0.0
opencv-python-headless>=4.9.0,<5.0.0
Pillow>=10.3.0,<11.0.0
reportlab>=4.2.0,<5.0.0
streamlit-drawable-canvas==0.9.3
psycopg2-binary>=2.9.9,<3.0.0
requests>=2.31.0,<3.0.0
cryptography>=42.0.0,<44.0.0
boto3>=1.34.0,<2.0.0
pydantic>=2.7.0,<3.0.0
email-validator>=2.1.0,<3.0.0
streamlit-passwordless>=0.2.0

# API REST (FastAPI) — roda ao lado do Streamlit, base do frontend/apps.
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
httpx>=0.27.0,<1.0.0
PyJWT>=2.8.0,<3.0.0
python-multipart>=0.0.9,<1.0.0
# Login por biometria (WebAuthn/passkeys) — opcional em runtime: a API
# funciona sem, respondendo 503 nos endpoints de passkey.
webauthn>=2.5.0,<3.0.0

# Consultor com IA (opcional). Com o SDK instalado, definir ANTHROPIC_API_KEY
# no servico da API liga o modo IA (Claude); sem a chave, o consultor segue
# em modo offline por regras. O SDK e leve e so e usado quando ha chave.
anthropic>=0.40.0,<1.0.0
# openai>=1.50.0  # descomente se preferir OpenAI (OPENAI_API_KEY)
